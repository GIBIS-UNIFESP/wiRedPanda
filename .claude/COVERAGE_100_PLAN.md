# Coverage → 100% Tracking

Master plan: directory-by-directory sweep, real behavioral tests only, no
weak assertions, no blanket exclusions. See conversation/plan for full ground
rules. `App/Resources/**` and `MCP/Client/**` are excluded per `codecov.yml`
and out of scope here.

Baseline (Codecov, master, checked 2026-07-19): 89.6% overall. App 80.84%,
Tests 95.66%, MCP/Server 63.71%.

## Sweep order & status

| # | Directory | Status | Notes |
|---|-----------|--------|-------|
| 1 | App/BeWavedDolphin/ | **done** | Every file 100% (176/176 functions), 18/18 files. Fixed 5 real bugs total: 4 null-deref crashes on nullable `m_host`, plus `WaveformSimulator::restoreInputs()` corrupting `InputRotary`'s one-hot dial position instead of restoring it. |
| 2 | App/CodeGen/ | **done** | Every file 100% (ArduinoCodeGen.cpp/.h, SystemVerilogCodeGen.cpp/.h, CodeGenUtils.h). SystemVerilogCodeGen.cpp went 76%→100% via 12 new unit tests + ~30 documented exclusions; fixed a gcov multi-line-statement false-miss (chained `.arg()` calls) by reformatting onto one line. No production bugs found — this file's rewrite already carries `[BUG-N]`/`[QUALITY-N]` fix annotations from an earlier pass. |
| 3 | App/Core/ | **done*** | Every file 100% except `Application.h`'s `guardedSlot<Body>` template (50%, 74 call sites across `App/UI/*.cpp` — its own instantiation is tested directly, but the 74 production instantiations live in App/UI/) and `StepEngineCore.h` (67%, shared by `ExerciseEngine`/`TourEngine` in App/Exercise/ and App/Tour/). Both **deferred by design** (see Session notes) — pick back up when the sweep reaches App/UI/, App/Exercise/, App/Tour/. |
| 4 | App/Element/ | **done** | Every file 100%. Full list: everything in `App/Element/GraphicElements/*` (60+ files), `ElementFactory.cpp/.h`, `ElementPorts.cpp`, `ElementMetadata.cpp`, `ElementSimState.cpp`, `ElementLabel.cpp`, `ElementAppearance.cpp`, `GraphicElementSerializer.cpp/.h`, `IC.cpp/.h`, `ICLoader.cpp/.h`, `ICPreviewPopup.cpp/.h`, `ICRenderer.cpp`, `ICSimulation.cpp`, `GraphicElement.cpp/.h`. Directory total: 4172/4172 lines. |
| 5 | App/Exercise/ | **done** | `ExerciseEngine.cpp/.h` and `ExerciseOverlay.cpp/.h` (new dedicated tests) both 100%. `StepEngineCore.h` rose 67%→83% as a side effect; remaining gaps are cleanly isolated to the `TourStep` instantiation — pick up in App/Tour/. |
| 6 | App/IO/ | **done** | Every file 100%: `ExternalFilePath.h`, `FileUtils.h`, `RecentFiles.cpp`, `Serialization.cpp/.h`, `VersionInfo.h`. `Serialization.cpp` (89%→100%) was the big one — extensive fuzz-hardening/bounded-reader guards, all closed with hand-built truncated/malformed streams. |
| 7 | App/Scene/ | in progress | Done: VisibilityManager.cpp, SceneItemRegistry.cpp, InlineLabelEditor.cpp, PortHoverLabel.cpp, PropertyShortcutHandler.cpp, ClipboardManager.cpp, ConnectionManager.cpp, GraphicsView.cpp, ICRegistry.cpp, SceneInteraction.cpp, SceneDropHandler.cpp (new TestSceneDropHandler class), Commands.cpp, Workspace.cpp. Remaining: Scene.cpp (710 lines, 89%, largest file — last one in this directory) |
| 8 | App/Simulation/ | not started | |
| 9 | App/Tour/ | not started | |
| 10 | App/UI/ | not started | LanguageManager.cpp (73.2%, has test file) |
| 11 | App/Wiring/ | not started | |
| 12 | MCP/Server/Core/ | not started | MCPValidator.cpp (56.7%) |
| 13 | MCP/Server/Handlers/ | not started | ThemeHandler/ServerInfoHandler/ConnectionHandler/HistoryHandler/SimulationHandler have zero test files; FileHandler/ICHandler have partial (Security-focused) tests |
| 14 | Tests/** (dead branches in harness itself) | not started | e.g. TestSystemVerilogExport.cpp differential mode (50.4%) |

## Documented exclusions log

| File | Line | Justification |
|------|------|----------------|
| App/BeWavedDolphin/DolphinFile.cpp | `save()`'s commit-failure throw | Not deterministically reachable from a single-user test process: `QSaveFile::open()` already rejects a non-regular-file destination up front, so the remaining commit()-only failure modes (e.g. losing rename permission via a sticky-bit dir) need a second OS user or root. |
| App/BeWavedDolphin/BeWavedDolphin.cpp | `checkSave()`'s `default:` switch case | The dialog offers exactly 3 StandardButtons (Save/Discard/Cancel), so `QMessageBox::question()` can only return one of those — `default` can't be hit. |
| App/BeWavedDolphin/BeWavedDolphin.cpp | `exportToPng()`'s `catch (...)` block | `DolphinExporter::renderToPixmap()` only calls Qt widget/painting APIs (none throw) and `QPixmap::save()` reports failure via its bool return, not an exception. Kept as a defensive backstop; provably dead given the current call graph. |
| App/BeWavedDolphin/BeWavedDolphin.cpp | `snapshot()` / `allCellIndexes()` closing braces | Compiler-generated cleanup for local non-trivial-destructor objects (`WaveformSnapshot`, `QModelIndexList`) on an exception-unwind path neither function ever takes. |
| App/BeWavedDolphin/DolphinModelBuilder.cpp | `collect()`'s `if (!elm \|\| type() != GraphicElement::Type) continue;` | `Scene::elements()` (the only real caller) already filters via `qgraphicsitem_cast` to items whose `type() == GraphicElement::Type`, so this can't be null or the wrong type from a real `Scene`. |
| App/BeWavedDolphin/DolphinModelBuilder.cpp | `buildLabels()`'s closing brace (both template instantiations) | Same compiler-generated `QStringList` cleanup pattern as above. |
| App/BeWavedDolphin/Serializer.cpp | `loadBinary()`'s closing brace | Same pattern, for `WaveformData`. |
| App/BeWavedDolphin/WaveformSimulator.cpp | `captureInputs()`'s closing brace | Same pattern, for the returned `QVector<Status>`. |
| App/BeWavedDolphin/DolphinHost.h | `virtual ~DolphinHost() = default;` | Defaulted virtual destructor calls are inlined at each derived-class destruction site (`StubDolphinHost` etc. destruct constantly in tests) — gcov just never attributes an inlined call back to this declaration line. **Watch for this pattern recurring throughout the rest of the sweep** — any `= default` special member function is a likely false-miss candidate before assuming a real gap. |
| App/CodeGen/ArduinoCodeGen.cpp | `otherPortNameImpl()`'s null-port/null-connection/cycle-detection guards (~10 lines) | Every call site passes a real, connected `Port*`; a `Connection` in a port's own list always has 2 valid endpoints; the wireless-cycle `visited` re-check needs a deliberately-crafted multi-hop wireless/connection loop that no real or malformed-by-accident circuit produces. |
| App/CodeGen/ArduinoCodeGen.cpp | `isArduinoReserved(varName)` guards in `declareInputs()`/`declareOutputs()` (3 sites) | `varName` always has a numeric counter baked in (and gets `_`-prepended by `removeForbiddenChars()` if it'd start with a digit), so it can never collide exactly with a bare reserved word like `"output"` — no label choice can trigger this given the current naming scheme. |
| App/CodeGen/ArduinoCodeGen.cpp | Several `if (x.isEmpty())` fallbacks after calling `otherPortName()` | `otherPortName()`/`otherPortNameImpl()` has no return path that yields an empty string (every branch resolves to `"HIGH"`/`"LOW"`/a real variable) — these `isEmpty()` checks are always false. |
| App/CodeGen/ArduinoCodeGen.cpp | `assignVariablesRec()`'s and `assignLogicOperator()`'s "unsupported element type" switch arms | The calling switch already filters to only the types each function actually handles (IC is handled separately; every other listed type is input-only or output-only and was already filtered by an inputs()/outputs()-empty check upstream) — these arms can't be reached with the current element type set. |
| App/CodeGen/SystemVerilogCodeGen.cpp | `otherPortNameImpl()`'s second cycle-check (`visited.contains(otherPort)`) and `!elm` fallback | Every Connection joins one OutputPort to one InputPort (never same-kind) and every call site/recursion starts from an InputPort, so `visited` only ever holds InputPorts while `otherPort` here is always an OutputPort — can never match. `!elm` is dead too: every Port is bound via `setGraphicElement()` at construction, before it could ever be wired into a Connection. |
| App/CodeGen/SystemVerilogCodeGen.cpp | `otherPortNameImpl()`'s trailing `return "1'b0"` after the gate-type check | `declareAuxVariablesRec()` pre-populates `m_varMap` for every element type except the 8 gate types checked just above (top-level gates are inlined here by design; IC-internal gates get pre-declared wires) — `mapped` is never empty for a non-gate `elm` reaching this point. |
| App/CodeGen/SystemVerilogCodeGen.cpp | `collectICTypes()`'s empty-label fallbacks (`"in_%1"`/`"out_%1"`) | Empirically confirmed (not assumed): `ICLoader`'s `buildPortLabels()` falls back to the boundary element's class name (e.g. `"InputSwitch"`) whenever its label is empty — even an *explicitly* blanked `setLabel("")` gets overridden on reload. A loaded IC's port name is never genuinely empty. |
| App/CodeGen/SystemVerilogCodeGen.cpp | `findFeedbackElements()`'s `successors` lambda closing brace | Recurring pattern 1 (compiler-generated cleanup), for the returned `QVector<GraphicElement *>`. |
| App/CodeGen/SystemVerilogCodeGen.cpp | `generateSingleICModule()`'s 3 `if (!nodeElm) continue;` guards | `ICLoader::loadBoundaryElement()` always populates `internalInputs()`/`internalOutputs()` with a real proxy Node's port; a connected port's `graphicElement()` is never null (see above). |
| App/CodeGen/SystemVerilogCodeGen.cpp | `declareAuxVariablesRec()`'s `preMapped` insert/consume branches | This function runs once per element list (top-level or one IC's internals); the only ports pre-populated beforehand (top-level Inputs, IC boundary Nodes) are already filtered out before reaching the general assignment path — a fresh element's own output port can never already be mapped here. |
| App/CodeGen/SystemVerilogCodeGen.cpp | TruthTable's `outputs.isEmpty()` `break` | `TruthTable::minOutputSize()` is 1 — `outputs` is never empty. |
| App/CodeGen/SystemVerilogCodeGen.cpp | TruthTable's `"LOW"`/`"HIGH"`/`isEmpty()` disconnected-input checks, and the `outputVarName.isEmpty()` disconnected-output block (incl. a `throw`) | Same "never empty" finding as ArduinoCodeGen, re-derived for this file's own `otherPortName()`; this file's `highLow()` returns `"1'b1"`/`"1'b0"`, never the literal text `"LOW"`/`"HIGH"` (an ArduinoCodeGen-only convention). Output side: `declareAuxVariablesRec()` always pre-maps every TruthTable output. |
| App/CodeGen/SystemVerilogCodeGen.cpp | `assignVariablesRec()`'s and `generateLogicExpressionImpl()`'s "unsupported element type" switch arms (incl. a `throw`) and the dead `return ""` arms | Same upstream-already-filters-the-type-set reasoning as ArduinoCodeGen, confirmed independently for this file's two dispatch points (top-level gate branch + IC-branch + inputs/outputs-empty continue). |
| App/CodeGen/SystemVerilogCodeGen.cpp | `generateLogicExpressionImpl()`'s single-input branch and the negate-block's `expr.mid(1)` double-negation cancel | And/Or/Nand/Nor/Xor/Xnor all have `minInputSize()==2` (Not/Node — the only 1-input types — return early via their own dedicated cases) — `inputs().size()==1` never holds here, so `expr` always comes from the parenthesized multi-input branch and can never itself start with `"~"`. |
| App/CodeGen/SystemVerilogCodeGen.cpp | `loop()`'s `expr.isEmpty()` fallback | Same "otherPortName() never returns empty" finding as above. |
| App/CodeGen/CodeGenUtils.h | `removeForbiddenChars()`/`sanitizeComment()` closing braces | Recurring pattern 1, for the returned `QString`. |
| App/Core/Application.cpp | Font-registration failure branch in the constructor | `Application`/`QApplication` is a process-wide singleton constructed exactly once before any test runs — no test can corrupt the embedded font resource afterward to force `addApplicationFont()` to fail. |
| App/Core/Application.cpp | `makeExceptionInfo()`'s closing brace | Recurring pattern 1, for the returned `ExceptionInfo` (has `QString` members). |
| App/Core/ThemeManager.cpp | Constructor's Light/Dark `switch (m_theme)` case bodies | `ThemeManager::instance()` is a Meyer's singleton constructed exactly once, the first time anything in the whole test binary touches it — at that moment the (redirected) test settings store has no persisted theme yet, so `m_theme` is always `Theme::System` there. Recurring pattern 12. |
| App/Core/ThemeManager.cpp | `resolveSystemTheme()`'s `!qApp` fallback | `qApp` is always alive while any test runs. |
| App/Core/Priorities.h | `legacyCalculatePriorities()`'s "`current` already has a priority" re-check at the top of the inner `while` | Proven dead by the algorithm's own invariants, not assumed: a node is only pushed while absent from both `outPriorities` and `inStack`, and a node only leaves `inStack` together with either getting a priority (immediately followed by popping that same stack entry) or via this very check — so `stack.top()` can never already hold a priority. The outer `for` also never revisits an already-resolved node (its own `if (outPriorities.contains(element)) continue;` guard). |
| App/Core/UpdateChecker.cpp | `downloadProgress` lambda's `if (received > 1024*1024) { reply->abort(); }` | Empirically confirmed unreachable in normal operation (not assumed): a local `QTcpServer` test streaming well past 1 MiB while nothing reads from the reply shows `received` plateau exactly at `setReadBufferSize()`'s cap and never exceed it — Qt's own backpressure prevents it. Kept as a belt-and-suspenders guard for a network-backend timing edge case (a single delivery landing before backpressure engages) that a synchronous local-server test can't reproduce. |
| App/Element/GraphicElements/AudioBox.cpp | `startAudio()`'s `if (m_player->source().isEmpty())` fallback | `m_hasOutputDevice` never changes at runtime in production, so if the constructor's initial `setAudio(kDefaultAudioPath)` call ran at all, the source can't have gone empty by the time `startAudio()` (only reachable via `play()`) is called. |
| App/Element/GraphicElements/ToneGenerator.h | `writeData()` | The device is only ever opened `ReadOnly` (in `start()`); `QIODevice::write()` itself rejects the call before this override could run on a device not opened `WriteOnly`/`ReadWrite`. |
| App/Element/GraphicElements/Led.cpp | `colorIndex()`/`appearanceStates()`'s `switch (inputSize())` `default:` cases | `GraphicElement::setPortSize()` rejects any `inputSize()` request outside `[minInputSize(), maxInputSize()]` = `[1, 4]` as a no-op, so `inputSize()` can never be anything the explicit case labels don't already cover. |
| App/Element/GraphicElements/TruthTable.cpp | `updatePortsProperties()`'s two `if (!rotatesGraphic())` port-loop guards | `rotatesGraphic()` reads this type's compile-time metadata, which `TruthTable`'s `ElementInfo` never overrides away from the default (`true`) — permanently unreachable for this class specifically (other element types with `rotatesGraphic=false` do reach the equivalent code). |
| App/Element/ElementLabel.cpp | `mouseMoveEvent()`'s `if (!(buttons() & LeftButton))` guard | Empirically confirmed unreachable (not assumed): this widget never calls `setMouseTracking(true)`, and `QApplication`'s own event delivery filters out `MouseMove` events before dispatch when no button is held and tracking is off — confirmed by sending a directly-constructed no-button `QMouseEvent` and observing, via a temporary debug print, that `mouseMoveEvent()` was never even invoked (not just that the branch wasn't taken). |
| App/Element/ElementPorts.cpp | `takeLastInput()`/`takeLastOutput()`'s empty-vector guards | Sole caller (`GraphicElementSerializer`'s `removeSurplusInputs`/`removeSurplusOutputs`) has a loop condition that already prevents calling them once empty. |
| App/Element/ElementFactory.h | `instance()`'s Meyer's-singleton construction line | Same class of gcov gap as `ThemeManager::instance()` (pattern 12), but more extreme: the very first call happens during *static initialization* (every element's file-scope registration lambda calls it before `main()` runs), and gcov doesn't attribute a hit to code that runs that early. |
| App/Element/ICLoader.cpp | `comparePorts()`/`buildPortLabels()`'s null-`graphicElement()` guards | `ElementPorts::addPort()` unconditionally calls `setGraphicElement(m_owner)` right after constructing a port — every port reachable from these functions already has a non-null owner. |
| App/Element/ICLoader.cpp | `processLoadedItems()`'s post-Connection-check `if (!elm) continue;` | `Serialization::deserialize()` only ever produces `Connection` or `GraphicElement` items; the Connection case above already handled the only other possibility. |
| App/Element/ICLoader.cpp | `migrateFile()`'s `QSaveFile::commit()` failure throw | Same class as `DolphinFile::save()`'s existing exclusion — `open()` already rejects the deterministic failure modes; the remaining ones need a second OS user or root. |
| App/Element/ICLoader.cpp | `deserializeAndLoad()`'s itemsGuard Connection-cleanup body | Unlike `loadFileDirectly()` (whose `migrateFile()` call can throw between items being populated and drained, genuinely needing this), nothing on this path can currently throw in that window — `resetInternalState()` only asserts already-guaranteed invariants, and `processLoadedItems()`'s one step before its drain loop (`generatePreviewPixmap()`) has no throwing path of its own. Kept for structural symmetry with the file-based guard. |
| App/Element/GraphicElement.cpp | `~GraphicElement() = default` | Same destructor-ABI-variant gcov gap as `DolphinHost.h`'s destructor (pattern 3), for a virtual dtor this time — Itanium ABI emits separate deleting/complete-object variants gcov attributes to one line. |
| App/Element/GraphicElement.cpp | `editableProperties()`'s closing brace | Recurring pattern 1 (compiler-generated `QList` cleanup). |
| App/IO/Serialization.cpp | `readBoundedString`/`readBoundedByteArray`/`readBoundedStringList`/`readBoundedKeySequence`/`readBoundedBitArray`/`readBoundedVariant`'s fallback/`readBoundedBlobMap`'s closing braces | Recurring pattern 1 (compiler-generated cleanup for the local returned by value), ~6 sites. |
| App/IO/Serialization.cpp | `deserialize()`'s post-connection-load stream-status check | `ConnectionSerializer::load()` has no path that returns normally with a bad stream status — both its `hasConnectionQMap` branches (QMap read, raw id1/id2 read) explicitly re-check status and throw their own, more specific `Pandaception` first, and nothing is read from the stream afterward. Defensive mirror of the element-load check above it, for a failure mode `Connection::load()` cannot currently produce. |
| App/IO/Serialization.cpp | `typeName()`'s static `QHash` initializer-list entries | Recurring pattern 12 (Meyer's-singleton-style one-time construction) — the first-ever call's static-init happens inside a compiler-generated guard gcov doesn't attribute to the visible initializer lines, even though `typeName()`'s correct output proves they ran. |
| App/Scene/VisibilityManager.cpp | `showGates()`'s null-cast guard | Same "every `Port` has a non-null owner" invariant as `ICLoader.cpp` (`ElementPorts::addPort()` sets it unconditionally at construction) — applies equally here. |
| App/Scene/SceneItemRegistry.cpp | `registerItem()`/`unregisterItem()`/`forget()`'s null-item guards (3 sites) | Every call site is traced back to a real, just-constructed/still-alive `QGraphicsItem*` (never a null pointer threaded through from elsewhere) — confirmed per call site, not assumed from the pattern alone. |
| App/Scene/ConnectionManager.cpp | `sideFor()`'s null-element guard, `encodePortIndex()`'s `-1` fallback | Same port-always-has-an-owner invariant as above; `encodePortIndex()`'s fallback is fed only by ports already resolved from a real element's own port list, so the "not found" case can't occur. |
| App/Scene/ConnectionManager.cpp | `connectedPeers()`'s closing brace | Recurring pattern 1 (compiler-generated cleanup for the returned container). |
| App/Scene/Commands.cpp | `UpdateCommand::wirelessStateDiffers()`'s size-mismatch guard | `GraphicElement::hasWirelessMode()` is purely type-based (`Node` always `true`, base class always `false`), never reads instance state `elm->load()` could change, and the command's `elements()` list is the same fixed ID set on both sides of the comparison — the set of wireless-capable elements can't change size between the before/after snapshots. |
| App/Scene/Commands.cpp | `SplitCommand` constructor's two throws (`!startPort \|\| !endPort`, `!startElement \|\| !endElement`) | Both current callers (`SceneInteraction::mouseDoubleClick()`, MCP `ConnectionHandler::handleSplitConnection()`) already require a matched connection with real, owned ports before constructing this command. |
| App/Scene/Commands.cpp | `MorphCommand::transferConnections()`'s `hasAudio()`/`hasDelay()` transfer lines | No element type currently sets `hasAudio=true` in its `ElementInfo` (confirmed by grep across every `GraphicElements/*.cpp`), and `Clock` is the only type with `hasDelay=true` — no two distinct types can both satisfy either guard. |
| App/Scene/Commands.cpp | `FlipCommand` constructor's closing brace, `UpdateBlobCommand::captureConnections()`'s closing brace | Recurring pattern 1 (compiler-generated cleanup), confirmed both the early-return and full-body paths are independently exercised by other tests. |
| App/Scene/Workspace.cpp | `save()`'s and `autosave()`'s `QSaveFile::commit()` failure throws (2 sites) | Same class as `ICRegistry::extractToFile()`'s/`DolphinFile::save()`'s existing exclusions — `open()` already rejects the deterministic failure modes; the remaining ones need a second OS user or root. |
| App/Scene/Workspace.cpp | `setAutosaveFileName()`'s and `autosave()`'s `mkpath()` calls (2 sites) | The global AppData autosaves directory is created by the *first* `WorkSpace` constructed in the whole test process (every `WorkSpace` calls `setAutosaveFileName()`), so by the time any later test runs, `!path.exists()` is already false — a Meyer's-singleton-style one-time branch (pattern 12) that can't be isolated without disrupting shared state other tests rely on. Confirmed empirically for the project-dir variant: `QFileInfo::isWritable()` on a not-yet-created directory returns `false`, so that branch never even selects a directory needing `mkpath()`. |
| App/Scene/Workspace.cpp | `onChildICBlobSaved()`'s `targets.isEmpty()` guard | `elm` (just confirmed embedded with this exact blob name, and already resolved as a live scene element two lines above) trivially satisfies `ICRegistry::findICsByBlobName()`'s own identical `isEmbedded()`+blobName-match scan over the same scene, so `targets` always contains at least `elm` itself. |
| App/Scene/ICRegistry.cpp | `findICsByBlobName()`'s closing brace | Recurring pattern 1 (compiler-generated cleanup for the returned `QList<GraphicElement *>`). |
| App/Scene/ICRegistry.cpp | `extractToFile()`'s `QSaveFile::commit()` failure throw | Same class as `DolphinFile::save()`'s existing exclusion — `open()` already rejects the deterministic failure modes; the remaining ones need a second OS user or root. |
| App/Scene/ICRegistry.cpp | `rollbackElements()`'s `elm->load(stream, ctx)` call | `reloadTargetsAtomically()`'s three current callers (`onFileChanged`/`embedICsByFile`/`extractToFile`) all apply the *same* file or blob bytes to every target in one batch, and a parse failure depends only on those bytes, never on which target is being mutated — so within one batch either every target succeeds or the first one throws immediately. The already-mutated subset needing rollback can therefore never be non-empty via any current caller. |

**Recurring exclusion patterns to recognize on sight** (don't re-derive from scratch each time):
1. A function's closing `}` shown as uncovered when the function itself clearly ran → compiler-generated cleanup for a local non-trivial-destructor object's exception-unwind path that's never taken. Exclude the closing brace line.
2. A `qCDebug`/`qCWarning` call split across multiple physical lines shows the first line covered but continuation lines `#####` even when the log call is definitely reached → the streamed arguments only evaluate when the category is enabled (default verbosity is 0 in tests), and gcov only credits the *first* line for the always-evaluated enabled-check. Fix: reformat onto one physical line (matches this codebase's existing single-line convention anyway) rather than fiddling with `Comment::setVerbosity()` in tests.
3. `= default` special member functions (esp. virtual destructors) may show as uncovered even when heavily exercised, since the trivial body gets inlined at each call site with no attributable standalone execution. Exclude with a one-line justification rather than chasing it.
4. Before writing a test for an `if (someString.isEmpty())` fallback fed by a helper function, check every return path of that helper first — if it has no path that returns an empty string (common for "resolve to a name or a sensible default" helpers), the fallback is dead and no circuit will ever trigger it.
5. A variable name built as `prefix + counter + ...` can never exactly equal a bare reserved/keyword string — don't spend time hunting a label that would trigger an `isReserved(fullVarName)`-style check if the counter is unconditionally part of that name.
6. When constructing a "run out of a shared resource" test (pins, IDs, etc.) with a MIX of element types, scene iteration order (`QGraphicsScene::items()`, roughly most-recently-added-first) determines which element's own overflow check actually fires — order assumptions are easy to get backwards. The robust fix: make every element competing for the resource the *same type*, so whichever one is starved when the resource runs out throws from the identical source line regardless of order.
7. A `GraphicElement`'s label is *never* genuinely empty once it round-trips through save+load as an IC boundary port — `ICLoader::buildPortLabels()` falls back to the class name (e.g. `"InputSwitch"`) whenever the label is blank, even an explicit `setLabel("")`. Don't assume "leave it unlabeled" reaches an `isEmpty()`/`"_unnamed"` fallback; verify with a `qWarning()` probe first (see the IC dedup test in `TestSystemVerilogCodeGenUnit`) before writing the test or excluding the line.
8. A multi-line statement built from chained calls (`.arg().arg()...`, not just streamed `qCDebug`/`qCWarning`) can show its *first* line as 0-hits while the continuation lines show real counts — the opposite direction from pattern 2, but the same root cause (gcov attributing the statement's "entry" oddly across lines). Fix: reformat onto one physical line; don't assume the surrounding loop is unreached just because the first line reads `#####`.
9. To build a *real* multi-level IC fixture for a unit test (not an integration `.panda` file), construct the sub-circuit directly (`WorkSpace`/`CircuitBuilder`), call `workspace.save(path)`, then `IC::loadFile(path, dir)` — this round-trips through the actual production save/load path (real port-label derivation, real boundary-Node proxying) without needing to hand-craft a `.panda` blob. This is the reusable "minimal IC" helper technique deferred during `ArduinoCodeGen.cpp` and used to close out `SystemVerilogCodeGen.cpp`'s IC-reserved-word/dedup gap.
10. Before assuming an out-of-topological-order element reference is dead/impossible, check whether the surrounding code has an explicit recursive fallback for it (e.g. `otherPortNameImpl()` recursing into `generateLogicExpressionImpl()` when an upstream gate hasn't been assigned yet) — if so, it's live defensive-but-real code for when `Scene`/caller-supplied element order isn't strictly topological, and deserves a real test (build the elements vector directly, order it deliberately), not an exclusion.
11. A `Q_ENUM`/`Q_GADGET` enum's moc-generated reflection registration can have *several* distinct compiled entry points attributed back to the same `Q_ENUM(Name)` source line — one `QMetaEnum` API alone (e.g. just `valueToKey()`) may leave others (`keyToValue()`, `key(int)`, and critically a `qDebug() << value;` stream call) uncovered. Exercise all four before concluding any remainder is unreachable.
12. A **Meyer's singleton** (`static T instance; return instance;` inside a function, e.g. `ThemeManager::instance()`) is constructed exactly once, at whatever moment the whole test binary first touches it — usually very early, well before any test gets a chance to control the global/settings state the constructor reads. Constructor branches keyed on that first-read state (e.g. "was a theme already persisted?") are architecturally locked out from later tests, not dead code — document why, don't chase them, and don't add a test-only reset hook (that's a behavior change, not a test seam).
13. Before assuming a "received/progress exceeds a configured buffer cap" branch is reachable, check empirically with a real local `QTcpServer`/similar test first: `QIODevice`-based backpressure (e.g. `QNetworkReply::setReadBufferSize()`) can make Qt itself refuse to admit more than the cap when nothing drains the device, so the counter may provably never exceed the configured limit — don't assume the surrounding code's own comment about the intended failure mode is achievable from a synchronous test.
14. When a newly-added constructor/friend/member touches a header that's included very widely (e.g. `Application.h`, `Enums.h`), expect the next `cmake --build` to recompile a large fraction of the codebase — this is normal, not a sign something is wrong; let it run in the foreground or background and don't restart it.
15. A shared `m_hasOutputDevice`-style hardware-detection flag (real audio/serial/etc. device present or not) makes coverage of its gated branches depend on whether the CI/sandbox machine actually has that hardware — this sandbox has a real (virtual) ALSA device, so it's always `true` here and could be `false` on a headless CI runner. Add a small friended test-only setter (mirrors `UpdateChecker::setApiUrlForTesting()`) to force the flag deterministically rather than relying on whatever the current machine happens to have.
16. Before assuming a version-gated old-format load branch (`!hasXxx(version)`) needs a real backward-compatibility fixture, check whether the feature it gates could even have existed in a file that old — if the element type itself was introduced at the same version the gate checks (e.g. `hasAudio()` gates both the version *and* the element's own introduction), no genuine `.panda` file predating it can ever contain one, and the branch is only reachable via a hand-built synthetic stream (or is a legitimate exclusion if building one isn't worth the effort). When it *is* worth building: mirror `GraphicElementSerializer::loadOldFormat()`'s exact field order (pos, rotation, label, 4×portsize, trigger, [unusedPriority], inputPorts, outputPorts, [appearanceNames]) and write `0` for port/appearance *counts* to skip their loops entirely rather than hand-crafting valid per-port entries.
17. A hand-built old-format load test using a `":/..."` resource-path string as the "bare path" payload silently no-ops: `ExternalFilePath::forReading()` always returns `std::nullopt` for a resource path under `PortableFile` purpose (by design — a stale saved resource identifier must not clobber the current compiled-in default), so the test passes without ever reaching the `setAudio()`/`setColor()`/etc. call it's meant to cover if the element's default happens to coincidentally match. Use a real absolute (e.g. `QTemporaryFile`) path instead, and assert the loaded value actually changed to it.
18. `resetSimState()` bypasses `setOutputValue()`'s change-detection entirely (it mutates `ElementSimState`'s output vector directly). `Simulation::update()`'s idle-tick-skip optimization only wakes on a clock/input-element's own `outputChanged()` flag, so calling `resetSimState()` directly then a bare `sim->update()` with nothing else touched gets silently swallowed — not a bug, just not a scenario any real caller (`WaveformSimulator`, which always immediately drives real input changes right after its reset loop) produces. Tests must do the same: drive a real input change immediately after calling `resetSimState()` directly.
19. `QDrag::exec()` (native drag-and-drop) is not automatically unsafe to call in a headless-ish test environment — confirmed empirically here (a real X11 `DISPLAY` is set, not `offscreen`) that it returns immediately rather than blocking, since there's no real held mouse button to sustain a drag session. Don't assume it hangs; verify first with a `timeout`-wrapped isolated run before deciding to exclude a `startDrag()`-reaching code path instead of testing it directly.
20. Before writing a test for a `QGraphicsItem`'s protected `mousePressEvent()`/`mouseMoveEvent()`/etc., check whether it's actually a `QWidget` (like `ElementLabel`, a `QFrame`) rather than a scene item — for widgets, `QCoreApplication::sendEvent(&widget, &qMouseEvent)` reaches the protected handler directly (no scene/view needed), but a **no-button `MouseMove`** sent this way may still never arrive: `QApplication`'s own event delivery (not just the widget) filters out untracked, no-button moves before dispatch. Confirm with a temporary debug print if a "no button held" branch stays stubbornly uncovered despite the send — it may be a genuine (if surprising) unreachable branch, not a mistake in the test.
21. Unlike a `QWidget`, a real `QGraphicsItem`/`QGraphicsObject`'s `sceneEvent()` is **not** reachable via `QCoreApplication::sendEvent(item, &event)` — that only calls `QObject::event()`, which for a graphics item does not forward to `sceneEvent()`. A `QGraphicsSceneMousePress` sent straight to a bare `Scene` (no view) also silently goes nowhere: `QGraphicsScene::event()`'s own mouse-press handling needs a real `QGraphicsView`/viewport in the loop (confirmed by a temporary debug probe showing zero dispatch either way). The one proven-reliable route for a real press/release is `QTest::mousePress(view->viewport(), button, modifiers, pos)` through an actual `MainWindow`+`GraphicsView` (see `testInputButtonMousePressReleaseIsMomentary`). If even that is unreliable for a specific modifier combination (see pattern 22), the fallback is a small `friend class TestX;` seam calling the protected `sceneEvent()` directly with a hand-built event — same trade-off as the existing `GraphicElementSerializer`/`IC`/`ICPreviewPopup` friend seams.
22. This sandbox's offscreen/X11 setup does not reliably reproduce a *specific* requested `Qt::KeyboardModifier` on a `QTest::mousePress(..., modifier, ...)`-synthesized scene event — empirically, requesting `Qt::ShiftModifier` arrived at `sceneEvent()` as `Qt::ControlModifier` instead (confirmed via temporary debug probes at the widget, scene, and item level), while `Qt::ControlModifier` itself sometimes didn't reach the item at all. Don't spend time debugging *why*; for a guard keyed on a specific modifier, prefer the direct-`sceneEvent()`-call friend seam (pattern 21) over chasing real synthetic input.
23. `Application::renderingEnabled` is `false` throughout the entire test suite (tests never paint), so `Connection::updatePath()` — and any similarly rendering-gated code — is a genuine, unconditional no-op in tests. A geometry-based assertion after moving a wire endpoint (`boundingRect()`/`sceneBoundingRect()` containing an expected point) will silently fail even when the underlying logic is correct, because the path never actually rebuilds. Use a behavioral assertion instead (which branch/state was reached — e.g. `hasEditedConnection()`/`editedConnection()->endPort()` — not the resulting paint geometry).
24. A Ctrl/Shift-modifier `QGraphicsSceneMouseEvent` synthesized via `QTest::mousePress`/`mouseClick` doesn't reliably carry the modifier through this sandbox's event pipeline — confirmed again (beyond the earlier `sceneEvent()`-specific pattern 22 finding) for a plain `Scene::mousePressEvent()` reached the normal way, through a real `GraphicsView`'s viewport. When a guard is keyed on a specific modifier and a direct `sceneEvent()` friend seam isn't applicable (the handler is a `QGraphicsScene` override, not a `QGraphicsItem` one), add a narrow `friend class TestX;` grant so the test can call the protected event handler directly with a hand-built event whose `setModifiers()` is set programmatically — same trade-off as the existing `sceneEvent()` friend seams.
25. `SceneInteraction::mouseDoubleClick()` (and potentially other double-click handlers) look up "what's under the cursor" via their own internally-tracked last-mouse-position field (updated only by `mousePress`/`mouseMove`), not the double-click event's own position. `QTest::mouseDClick()` alone doesn't reliably prime that field first — send a real `QTest::mouseClick()` at the same point immediately before the `mouseDClick()` call.
26. A wire-detach operation (`ConnectionManager::detach()`) deletes the old completed `Connection` and immediately starts a *new* in-progress one from the same output port — a raw `TestUtils::countConnections()` before/after comparison can't tell these apart (both count as 1 real `Connection` item on the scene), and reads as "detach did nothing" even when it worked correctly. Assert on the *specific* objects instead: the original input port's `connections()` is empty, and the new `editedConnection()->startPort()` matches the original wire's source.
27. If a `cmake --build` gets interrupted mid-compile (e.g. an accidentally-stacked duplicate invocation gets killed), check for **zero-byte `.gcno` files** afterward (`find build-coverage -name '*.gcno' -size 0`) before trusting the next `gcovr` run — `ccache` can serve a corrupted/incomplete cached `.gcno` for the file that was mid-compile at kill time even though its `.o` compiles and links fine, silently breaking `gcovr` for that one file (and, if the cache is near its size cap, sometimes more than one, discovered one at a time as gcovr processes files). Fix: delete the affected `.o`/`.gcno`/`.gcda`, rebuild once with `CCACHE_DISABLE=1` to confirm, then (if the cache looks generally unhealthy — check `ccache -s`) `ccache -C` and do one full `rm -f **/*.gcno **/*.gcda **/*.o` + rebuild to guarantee every file's instrumentation data is self-consistent before trusting the coverage numbers. Root cause of the corruption in the first place: never re-run a build command before confirming the previous one finished (see standing rule against stacking background builds) — this cost real time to track down.

## Session notes

- 2026-07-19: Plan approved. Tracking doc created.
- 2026-07-19: Fixed a pre-existing build breakage blocking the coverage preset entirely (Qt 6.16 deprecated `QDragEnterEvent(QPoint, ...)`, 6 call sites across 3 test files) — separate commit, not part of the sweep itself.
- 2026-07-19: Discovered Codecov does not honor `LCOV_EXCL_LINE` on the raw-`gcov`-upload path this repo used — switched `.github/workflows/coverage.yml` to generate the report via `gcovr` (which does honor those markers) instead, per user decision.
- 2026-07-19: `App/BeWavedDolphin/DolphinFile.cpp` done — 100%, one documented exclusion (see above). New `TestDolphinFile` test class added (14 tests), registered through all 4 required tiers (`CMakeSources.cmake` x2, `CMakeLists.txt` x2 — `add_test` + `ALL_TESTS`, `Tests/Runners/TestWiredpanda.cpp` x2).
- 2026-07-19: `App/BeWavedDolphin/BeWavedDolphin.cpp` done — 100%, three documented exclusions (see above). Found and fixed 4 real null-pointer-dereference crashes on `m_host` (documented/tested as nullable, but dereferenced unconditionally in `on_actionSaveAs_triggered()`, `associateToWiRedPanda()`, `createWaveform(fileName)`, and `on_actionLoad_triggered()`) — caught by two new tests that segfaulted before the fix. 30 new cases added to the existing `TestBewavedDolphinGui` (no new registration tiers needed, extended an existing class). Full suite (193 tests) passes; one pre-existing parallel-ctest flake (`TestSystemVerilogExport::testSystemVerilogExportClockGatedDecoder`) observed and confirmed unrelated (passes in isolation) — matches the known `project_flaky_parallel_tests` pattern, not a regression.
- 2026-07-19: `App/BeWavedDolphin/` **fully done** — every file 100%. Closed out DolphinModelBuilder.cpp, DolphinExporter.cpp, Serializer.cpp, SignalDelegate.cpp, DolphinClipboard.cpp, WaveformSimulator.cpp, DolphinZoom.cpp, DolphinEdits.cpp, plus the last two header lines, via 6 new test classes and additions to 3 existing ones. Found and fixed a real bug: `WaveformSimulator::restoreInputs()` corrupted `InputRotary`'s dial position (one-hot: selecting any port deselects the others) instead of restoring it, since it unconditionally called `setOn(value, port)` for every port rather than only the one actually captured as active — caught by a new test that failed before the fix. Discovered and documented two recurring gcov false-miss patterns (compiler-generated cleanup braces, multi-line qCWarning/qCDebug attribution) — see the exclusion table above; expect both again in later directories. Full suite (199 tests) green.
- 2026-07-19: User confirmed "same rigor, expect it's slower" for App/CodeGen/ (code generators verified partly by differential iverilog/avr-gcc runs in `Tests/Integration/`). In practice, the existing `TestArduinoCodeGenUnit`/`TestSystemVerilogCodeGenUnit` "Unit" tests are much lighter than that — they build a circuit directly via `CircuitBuilder`, call `generate()`, and assert on the emitted text, no toolchain needed. That pattern covered nearly everything.
- 2026-07-19: `App/CodeGen/ArduinoCodeGen.cpp` done — 100% (44/44 functions). 9 new tests (SR latch, sequential + non-sequential testbench generation, 4-output demux, VCC-tied TruthTable input, unwritable testbench path, pin-overflow). ~35 lines documented-excluded (see table above) — the biggest lesson: `otherPortName()`/`otherPortNameImpl()` provably never returns an empty string, which makes a whole family of `if (x.isEmpty())` fallbacks throughout this file dead code, and `assignVariablesRec()`'s/`assignLogicOperator()`'s "unsupported type" switch arms are dead because the dispatching switch upstream already filters to only the types each one handles.
- 2026-07-19: **Deferred** (not excluded — believed reachable in principle, just needs infrastructure not built yet): `ArduinoCodeGen.cpp`'s IC-external-output-port-naming fallback (unnamed port) and Node-inside-IC boundary resolution. Both need a real multi-level IC fixture (internalElements() populated, boundary ports wired) — none of the existing unit tests construct one programmatically; the only precedent (`TestComponents.cpp`'s `new IC()`) doesn't populate internals either. Come back to this once a lightweight "build a minimal IC with N internal elements" test helper exists (worth adding once needed by a second file, e.g. SystemVerilogCodeGen.cpp likely has the same gap).
- 2026-07-19: Environment note — two consecutive full-suite `ctest` runs launched with `run_in_background: true` were killed by the harness partway through (both had 100% pass rate up to the kill point, not a real failure). Foreground (blocking) `ctest` runs completed normally both times. If this recurs, prefer foreground for the full-suite verification step specifically.
- 2026-07-19: `App/CodeGen/` **fully done** — every file 100% (ArduinoCodeGen.cpp/.h, SystemVerilogCodeGen.cpp/.h, CodeGenUtils.h). `SystemVerilogCodeGen.cpp` went 76%→100%: 12 new unit tests (DLatch/SRLatch, all 4 flip-flop types across 3 async-control wiring scenarios, `ensureSimpleSignal()`'s wire-hoisting, two out-of-topological-order gate/Node resolution tests, a genuine Node-Node combinational cycle proving no infinite recursion, NAND, double-NOT cancellation, IC boundary reserved-word/dedup naming, Demux 2-select-line threshold, `moduleName()`) plus ~30 documented exclusions. Resolved the `ArduinoCodeGen.cpp`-deferred "minimal IC fixture" item via a `WorkSpace::save()` + `IC::loadFile()` round-trip (new recurring pattern 9) — this also definitively answered whether IC generation itself was under-tested: it wasn't, `Tests/Integration/TestSystemVerilogExport.cpp`'s 90+ real nested-IC `.panda` fixtures already exercise `collectICTypes`/`generateICModules`/`generateSingleICModule`/`findFeedbackElements` thoroughly; only the reserved-word/dedup/null-guard *edges* of that machinery were gaps. Found and fixed one more gcov false-miss variant (pattern 8: chained `.arg()` calls, not just streamed logging) in two Mux/Demux case-statement lines. No production bugs — every gap was either genuinely untested-but-reachable code or defensive/dead code, confirmed via call-graph analysis (e.g. `otherPortName()` provably never returns empty in this file either) and one empirical probe (label-defaulting behavior on IC boundary ports, confirmed via a temporary `qWarning()` before excluding).
- 2026-07-19: `App/Core/` **done** (17 files), with two templates explicitly deferred to later directories rather than forced here: `Application.h`'s `guardedSlot<Body>` (74 production call sites live in `App/UI/MainWindow.cpp`/`ICController.cpp`/`WorkspaceManager.cpp`/`ExportController.cpp`/`BeWavedDolphin.cpp` — each a distinct template instantiation gcov tracks separately; its own logic is still directly tested here) and `StepEngineCore.h` (shared by `ExerciseEngine`/`TourEngine`, gaps are those engines' own failure/edge-case paths). Key findings: (1) `App/Core/Priorities.h`'s legacy DFS "already resolved" recheck is dead by the algorithm's own push/inStack invariants — proven analytically, not by exhaustive graph-shape guessing, after several failed construction attempts. (2) `Q_ENUM`'s moc-generated reflection needs `valueToKey()` + `keyToValue()` + `key(int)` + a `qDebug()` stream call to fully exercise (pattern 11) — discovered by iterating one API at a time and rechecking gcov each round. (3) `ThemeManager`'s Meyer's-singleton constructor has a Light/Dark branch that's architecturally unreachable from any test (pattern 12) — the settings store has no persisted theme at the singleton's first-ever construction, which happens before any test can control it. (4) `UpdateChecker.cpp` needed a small, deliberate testability seam (injectable API URL, guarded by `friend class TestUpdateChecker`) to drive `checkForUpdates()`'s real request path against a local `QTcpServer` instead of the live GitHub API — this test also disproved the reachability of the `downloadProgress`-triggered abort() (pattern 13): `setReadBufferSize()`'s own backpressure caps `received` at the buffer size and it never exceeds it, confirmed by watching real `qWarning()` output across several debugging rounds (initial burst-write attempt produced *zero* progress signals; per-chunk-timer streaming showed `received` plateau exactly at the cap). Also caught and fixed a self-inflicted test bug (forgot to call `checker.checkForUpdates()` at all, so the test passed vacuously) before that finding was even reachable. (5) `QNetworkAccessManager::sslErrors` and `QStyleHints::colorSchemeChanged` were both triggerable directly via `QMetaObject::invokeMethod` on the real signal (same mechanism `emit` compiles to) — reusable technique for any Qt signal-driven callback with no production trigger available in a headless test. Application.h changing (widely included) triggered a full-project rebuild (pattern 14) — expected, not a problem.
- 2026-07-19/20: `App/Element/` in progress across a very long multi-part session (~25 commits total). **Fully done**: every file in `App/Element/GraphicElements/` (60+ files — all logic gates, all sequential elements, all displays, Mux/Demux/Node/Led/Line/Text/TruthTable/InputButton/InputRotary/Clock/AudioBox/Buzzer/AudioOutputElement/ToneGenerator), `ElementFactory.cpp/.h`, `ElementPorts.cpp`, `ElementMetadata.cpp`, `ElementSimState.cpp`, `ElementLabel.cpp`, `GraphicElementInput.h`, `ElementAppearance.cpp`, `GraphicElementSerializer.cpp/.h`, `IC.cpp/.h`, `ICPreviewPopup.cpp/.h`, `ICRenderer.cpp`, `ICSimulation.cpp`. Directory overall: 98.7%. Remaining: `ICLoader.cpp` (93%) and `GraphicElement.cpp/.h` (not yet profiled this pass — re-run gcovr fresh next session before starting).
  - Key findings/new patterns 15-20 (see above): (1) `AudioBox`/`Buzzer`'s hardware-detection flag needed a friended test seam since this sandbox has a real virtual ALSA device; (2) several pre-4.1/pre-feature-introduction old-format load paths (`Display7`, `AudioBox`/`Buzzer`/`Clock`/`Display14`/`Display16`/`InputRotary`) needed hand-built synthetic streams since no real fixture predates them, catching two real test-authoring mistakes along the way (a resource-path payload silently no-opping through `ExternalFilePath::forReading()`; a `resetSimState()`-then-bare-`update()` test swallowed by the simulation's idle-tick-skip optimization); (3) `InputRotary`'s port-count `default:` switch case turned out **genuinely reachable** (property-shortcut cycling passes through every size, not just "standard" ones) — real test, not an exclusion; (4) `InputButton`/`ElementLabel`'s mouse-event handlers were 0% covered — fixed via a real GUI press/release test and direct `QCoreApplication::sendEvent()` respectively, including empirically confirming `QDrag::exec()` doesn't hang in this real-X11 (non-offscreen) sandbox before exercising it directly; (5) `Node.cpp`'s wireless `Status::Error` case needed a genuine multi-driver bus conflict (real `Connection`s, not the `connectPredecessor()` shortcut).
  - `GraphicElementSerializer.cpp`/`IC.cpp` (the two hardest files this stretch) both needed a friended test-only seam to call private load-helper methods directly with minimal hand-built streams — precisely truncating a full stream at the exact byte offset each guard needs would have been far more fragile. Discovered mid-sweep that `QDataStream operator<<` for `QMap<QString,QVariant>`/`QList<QMap<...>>` is wire-compatible with this codebase's custom `readBoundedMetadata()`/`readPortList()` readers, so hand-built streams can just use Qt's own container serialization instead of manually writing counts/keys/values byte-by-byte — much less error-prone; use this for any further `GraphicElementSerializer`/`IC`/`ICLoader` stream-construction tests.
  - `IC.cpp`'s stale-portMap-key eviction (fuzz-hardening, mirrors the existing `bugB_ic_blob_shrink` fuzz fixture in `TestSerialization`) was reproduced directly: load a real multi-port sub-circuit, then feed `IC::load()` a stream whose own port lists match that count but whose "name" field points at a smaller sub-circuit, shrinking it out from under the just-captured saved keys.
  - No production bugs found this stretch (unlike earlier directories) — every gap was either a real, valuable test or a defensively-dead branch with a specific, checkable (often empirically-verified) reason.
- 2026-07-19/20 (continued): `App/Element/` **fully done** — closed out the last two files. `ICLoader.cpp` (93%→100%): real tests for the file-open failure, a genuine self-referencing-file circular reference (built by having a real IC load a small circuit then re-saving over the same file so `ExternalFilePath::forWriting()`'s bare-filename stripping makes the saved reference point at itself), a migration write-failure (locked directory + pre-created backup, mirroring `TestDolphinFile`'s established locked-dir technique), a multi-output boundary-input port-name test (`InputRotary`, 2 outputs by default), a verbosity-gated `qCDebug` line (bumped `Comment::setVerbosity(4)` around one load instead of excluding), and — the hardest one — a genuine 20-level-deep embedded-IC blob chain to trip `deserializeAndLoad()`'s own nesting-depth guard, built by writing directly into `ICRegistry::blobMapRef()` to bypass `ICRegistry`'s own, separate self-containment depth cap (same threshold, would otherwise reject construction before the target guard could ever be reached). Four lines excluded (see table); reasoning for the itemsGuard exclusion took real analysis of what can/can't throw between item-population and full-drain in that specific function — worth reading before assuming a similar-looking guard elsewhere is equally dead.
  `GraphicElement.cpp/.h` (99%→100%, the directory's last and largest file): real tests for `previousColor()`/`nextColor()`'s Blue/Purple/fallback branches (extended the existing `TestElementProperties::testColorCycling`), `isValid()`'s propagate-`Status::Error`-to-connected-outputs branch, the base no-op `setColor()`/`setAudio()`/`setVolume()`/`setFrequency()`/`setDelay()`/`blobName()` defaults (asserted the *getters* stay unchanged, not just "doesn't crash"), `colorNameToIndex()`'s unknown-color fallback, and `retranslate()` (previously 0% covered). `sceneEvent()`'s Ctrl+click swallow needed patterns 21-22 above — after real `QTest::mousePress` attempts proved unreliable for this specific modifier, added a new `friend class TestGraphicElement;` seam to call `sceneEvent()` directly with a hand-built `QGraphicsSceneMouseEvent`. Two exclusions (destructor ABI-variant gap, `editableProperties()` pattern-1 brace).
  Also hit and resolved a real environment incident this stretch (pattern 23): stacked two duplicate `cmake --build` invocations by mistake (didn't wait for the first to finish before re-running), had to kill both per direct user instruction ("stop both"), which left one translation unit's `.gcno` corrupted via a stale `ccache` entry — traced via `find -size 0`, fixed per-file with `CCACHE_DISABLE=1`, then (since `ccache -s` showed the cache at 99.78% of its 1 GB cap and more corrupted files kept surfacing one at a time) cleared the whole cache and did one full from-scratch rebuild to guarantee every `.gcno` in the tree is self-consistent before trusting the final coverage numbers.
- App/Element/ directory final state: 4172/4172 lines, 100%.
- 2026-07-20: `App/Exercise/` **done** — both files went from 56% combined to 100%. `ExerciseEngine.cpp` (57%→100%) had never tested `stop()`, `goToPreviousStep()`, or reaching the end of an exercise (`markCompleted()`/`exerciseCompleted()` were 0% covered) — existing tests only exercised load/retranslate/the always-fails unknown-type guard. Most importantly, the actual feature the engine exists for — auto-advancing when a real circuit change satisfies a step's element/connection requirement — had zero coverage; added the full happy-path (satisfied/insufficient-count/unknown-type, for both requirement kinds) using the established `AddItemsCommand`-via-`receiveCommand()` technique to trigger a real `Scene::circuitHasChanged()`. `ExerciseOverlay.cpp` (55%→100%) had no dedicated test file at all beyond a single static-method test; added full coverage using the existing `friend class TestExerciseEngine` seam for direct member access (widget children, `m_hintVisible`, `applyTheme()`'s colors) — no new friend grant needed. Two lines showed as uncovered despite being reached (pattern 8: chained `.arg()` on a `QPushButton` stylesheet string) — reformatted onto one line rather than chasing a test for them. Caught and fixed a self-inflicted test bug: two new fixture-builder helpers hardcoded the same exercise `"id"` across every call site, so `Settings::exerciseProgress()` — a real store persisted across the whole test process, not reset per test — carried step progress from one test into the next, causing several tests to start from step 1 instead of 0; fixed by deriving the id from each fixture's filename. `StepEngineCore.h` rose 67%→83%; every remaining gap belongs solely to the `TourStep` template instantiation (no more lines double-counted across both instantiations), confirming the `ExerciseStep` side is fully closed — deferred cleanly to App/Tour/.
- 2026-07-20: `App/IO/` **done** — every file 100%. `FileUtils.h`/`RecentFiles.cpp` were small (88%/89%→100%): `copyToDir()`'s two no-op guards, `filesHaveSameContent()`'s can't-open fallback (chmod technique), and `RecentFiles`'s `QFileSystemWatcher`-driven live cleanup (needed `QSignalSpy::wait()` since the connection is `Qt::QueuedConnection`). `Serialization.cpp` (89%→100%) was this directory's `SystemVerilogCodeGen.cpp`-scale file — dense fuzz-hardening/bounded-reader code guarding against OOM from crafted `.panda` files. Every implausible-size/count guard across `readVersionNumber`/`readBoundedByteArray`/`StringList`/`BitArray`/`Variant`/`Metadata`/`BlobMap`, `readPayload`, `readPandaHeader`/`readDolphinHeader` (no-device + malformed-legacy-header paths), and `copyPandaFile`'s peek-based plausibility pre-checks got a real hand-built truncated/malformed stream — all via direct calls to the (mostly-public) `Serialization::` static methods rather than going through a full `WorkSpace`/`Scene` round trip, since these are pure stream-parsing functions with no element-graph side effects to set up. Two real analytical findings, not just tests: (1) `deserialize()`'s post-connection-load status check is provably dead — `ConnectionSerializer::load()`'s two format branches both explicitly re-check status and throw their own more specific exception before returning, so the outer check can never see a bad status Connection::load() left behind (mirrors `App/Element`'s earlier `deserializeAndLoad()` itemsGuard finding: not every guard that *looks* symmetric with a working one is actually reachable — check what the callee can leave behind, don't assume from shape alone). (2) `typeName()` was 0% covered because its only production call site is a `qCDebug(three)` argument, which the disabled-by-default logging category never evaluates (same class as pattern 2, but for a whole function rather than a stream fragment) — fixed by calling it directly rather than fiddling with verbosity, since there's no other real behavior to exercise around it. Also found a NEW static-QHash-initializer gcov gap (typeName()'s own type map) — same root cause as pattern 12 (Meyer's-singleton one-time construction) but for a `static const QHash` aggregate rather than an `instance()`-returning singleton; documented as part of pattern 12 rather than a new numbered pattern, since the mechanism is identical.
- 2026-07-20: `App/Scene/` in progress. Done so far: `VisibilityManager.cpp`, `SceneItemRegistry.cpp`, `InlineLabelEditor.cpp` (all via `TestScene.cpp` additions — null-element guard, commit-previous-on-restart, empty-label bounding-rect fallback), `PortHoverLabel.cpp` (remaining geometry/paint surface not exercised via `ConnectionManager`), `PropertyShortcutHandler.cpp` (multi-element secondary/morph property cycling as single undo steps), `ClipboardManager.cpp` (no-selection no-ops, legacy clipboard mime-type fallbacks — built a real legacy-format blob via `WorkSpace::save(QDataStream&)` rather than an arbitrary byte string, since `RegisterBlobCommand::redo()` validates blob bytes as a real parseable panda file), and the largest of this batch, `ConnectionManager.cpp` (input-anchored wire completion/detach/cancel, hover-label rotated-port bias, stale-index guards, rejection-reason reporting) — ~18 new tests, extending the port-always-has-an-owner invariant (pattern from `ICLoader.cpp`) to two more files. New finding, pattern 23 above: `Connection::updatePath()` is a no-op under the test suite's `Application::renderingEnabled == false`, discovered after a geometry-based assertion silently failed twice on an otherwise-correct test; switched to a behavioral assertion instead. `GraphicsView.cpp` (82%→100%) done last: real middle-button-pan press/release, auto-repeat key-press/-release forwarding (`QApplication::sendEvent` direct to the widget), a real wheel-event zoom-out (zoom-in via wheel was already covered by `TestMainWindowGui`'s `wheelZoom()` helper), and `zoomToFit()`'s no-scene/selection-only/non-Scene-fallback/empty-target branches — the last of which required discovering that a real `Scene` can *never* produce an empty items-bounding-rect (`SceneInteraction::attachToScene()` permanently adds a 1×1 rubber-band-selection item to every real scene), so that guard is only reachable via a genuinely bare `QGraphicsScene`. No production bugs found in this stretch — every gap was either a real missing test or defensively-dead code with a call-site-traced justification.
- 2026-07-20: `App/Scene/ICRegistry.cpp` done (87%→100%, 11 new tests). Closed `cachedFileBytes()`'s open-failure fallback, `onFileChanged()`'s rewatch/no-targets branches (via the established `QMetaObject::invokeMethod(reg, "onFileChanged", ...)` technique — private slots are reachable this way since meta-object dispatch bypasses C++ access control, already precedented in `TestICInline.cpp`), the previously **completely uncalled-by-any-test** `createEmbeddedIC()` (real production caller: MCP's `add_ic --inline` handler), `extractToFile()`'s open failure, and `makeBlobSelfContained()`/`renameBlobReference()`'s pre-metadata-format / already-embedded-dependency / duplicate-fileBackedICs / missing-or-unreadable-dependency-file branches — the latter two needed a `QVersionNumber(4,4)` hand-built blob (predates `V_4_5`'s metadata support) and an in-memory `buildBlobBytes()` helper mirroring the wire-compatible-QDataStream-container trick from `App/Element/`'s `GraphicElementSerializer` work. Found and fixed one real bug: `embedICsByFile()`'s reload lambda called `ic->setBlobName()` *before* `ic->loadFromBlob()`, so a load failure left the target IC half-mutated (blob name changed, internal circuit untouched) with no rollback covering that field, since `reloadTargetsAtomically()` only rolls back targets whose `mutate()` call fully completed — reordered so `loadFromBlob()` runs first. Also derived (not guessed) a new structural-unreachability finding: `rollbackElements()`'s per-element `load()` call can never execute via any of the three current `reloadTargetsAtomically()` callers, because each applies identical bytes/file to every target in a batch, so a batch can only ever succeed uniformly or fail on its first target — documented as an exclusion rather than forced. Directory total for this file: 272/272, 100%.
- 2026-07-20: `App/Scene/Commands.cpp` done (96%→100%, 12 new tests). Closed `MorphCommand`'s remaining property-transfer/connection-restore branches: Not↔Node's 16px position adjustment (both directions), Buzzer↔Clock frequency transfer (the *only* type pair sharing `hasFrequency` — confirmed by grepping every `ElementInfo` initializer, not assumed), an And→Display7 morph dropping the output connection (Display7 has 0 outputs) and restoring it on undo (the output-side, `isInput==false` half of `restoreDeletedConnections()`), and a variant where the connection's peer element is deleted before the undo, hitting the "element no longer found, skip" guard. Also closed `UpdateCommand`/`UpdateBlobCommand`/`FlipCommand`'s empty-target early returns (all three constructed directly with `{}`, a legitimate public-API usage no current caller happens to exercise), and `SplitCommand`'s remaining `redo()`/`undo()` upstream-lookup throws plus `CommandUtils::findItems()`/`loadItems()`'s own stale-ID/count-mismatch guards — reusing `testSplitCommandRedoThrowsBeforeAllocation`'s established `scene->forgetItemId()` technique (drops the ID-registry entry without freeing the object) to corrupt scene state independently of the undo stack, calling command `redo()`/`undo()` directly rather than through `QUndoStack` (same macOS-arm64 exception-unwind caveat as that test). Five exclusions this file, each backed by a real check rather than a guess: `wirelessStateDiffers()`'s size-mismatch guard (`hasWirelessMode()` is purely type-based, can't change under `load()`), `SplitCommand`'s two constructor throws (both current callers pre-validate), `hasAudio()`/`hasDelay()`'s transfer lines (grepped — no type has `hasAudio=true`; only `Clock` has `hasDelay=true`), and two pattern-1 closing braces. No production bugs found. Directory total for this file: 711/711, 100%.
- 2026-07-20: `App/Scene/Workspace.cpp` done (86%→100%, 15 new tests). `WorkSpace` already granted `friend class TestWorkspaceUnit;` (from earlier minimap-geometry tests), so most of the new tests reach private members/methods (`m_fileInfo`, `m_loadedVersion`, `m_autosaveFileName`, `m_isInlineIC`, `autosave()`, `applyMinimapGeometry()`, `onMinimapGeometryChangeFinished()`, the `load(stream, version, contextDir)`/`resizeEvent()` overloads) directly rather than needing a new seam. Covers: resize repositioning a visible `ExerciseOverlay`, `save()`'s newer-version warn-and-no-op / associated-dolphin-file-copy / stale-autosave-cleanup branches, `load()`'s file-open failure / non-`.panda`-suffixed migration (verifies the re-saved file actually gets the suffix appended) / migration-target-read-only warning / both interactive-mode version dialogs (newer via a hand-built `QVersionNumber(999)` stream, older via `QVersionNumber(4,4)`), and `autosave()`'s inline-IC/newer-version skips, clean-stack cleanup, read-only-project-dir fallback, cross-directory stale-file cleanup, and open failure. Reused `Application::renderingEnabled`'s established toggle-and-restore pattern for `Application::interactiveMode`/`migrationEnabled` (both forced `false` globally in `TestUtils.cpp`'s suite setup) combined with `TestUtils::AutoDismisser::acceptMessageBox()` to dismiss the real, blocking `QMessageBox::warning()`/`::information()` calls those branches trigger — confirmed these are genuinely synchronous/blocking, so no `waitFor()` polling was needed after the call returns. One test needed a second iteration: locking a directory *before* a needed migration triggered `createVersionedBackup()`'s own write attempt to throw first (masking the target branch); fixed by pre-creating the exact versioned-backup filename so its own `QFile::exists()` check no-ops it before the lock takes effect. Four exclusions, each backed by a specific finding rather than a guess: two `commit()`-failure throws (same class as this sweep's other unwritable-without-root exclusions), two `mkpath()` calls that are only ever reached before any `WorkSpace` has run in the whole test process (confirmed empirically for one variant: `QFileInfo::isWritable()` on a not-yet-created directory returns `false`, so that branch never even selects a directory needing creation), and `onChildICBlobSaved()`'s empty-targets guard (the element itself always satisfies the identical lookup it's checking, by construction). No production bugs found. Directory total for this file: 464/464, 100%. This closes out every file in `App/Scene/` except `Scene.cpp` itself (710 lines, 89%, the largest single file in the directory).
- 2026-07-20: `App/Scene/SceneDropHandler.cpp` done (65%→100%) — had no dedicated test file at all. `handleNewElementDrop()`/`addFromMimeData()` were already exercised indirectly elsewhere (palette-drop tests), but `isSupportedDropFormat()` and `handleCloneDrag()` (Ctrl+drag clone within the scene) had zero coverage. New `TestSceneDropHandler` (registered through all 4 tiers: `CMakeSources.cmake` x2, `CMakeLists.txt` x2 — `add_test` + the `ALL_TESTS` FIXTURES_SETUP list — and `Tests/Runners/TestWiredpanda.cpp` x2) covers all four MIME formats, the unrecognized-format rejection, and a real clone-drag round trip (current + legacy MIME key) — built by hand-matching `ClipboardManager::serializeItems()`'s exact wire format (header + drag-start pos + selection centroid + serialized items) directly, since `SceneDropHandler`'s methods are all public and don't need a full `Scene` event-dispatch round trip (unlike `SceneInteraction`, which has none). One gotcha caught by a real failure, not guessed: grid-snap (`Constants::gridSize / 2` == 8px) perturbed a non-grid-aligned test position/offset, so the clone's expected landing position didn't match until both were changed to multiples of 8.
- 2026-07-20: `App/Scene/SceneInteraction.cpp` done (86%→100%, 8 new tests, no dedicated test file existed for it before — added to `TestScene.cpp` since it has no public accessor of its own and is only reachable through Scene's real mouse-event pipeline, same shape as the port-hover-label precedent). Covers: Ctrl+click selection toggle, completing an in-progress wire by pressing a second port mid-draw, starting/detaching a wire by pressing directly on an empty/occupied input port, right-click opening the context menu, the drag-past-50ms-throttle `ensureVisible()` scroll, and double-click wire-splitting (plus its Ctrl-held no-op guard). Two new patterns (24-26, see above): (1) Ctrl-modifier synthetic mouse events are unreliable even through a real `GraphicsView`, not just via `sceneEvent()` — fixed with a new `friend class TestScene;` seam on `Scene` calling `mousePressEvent()` directly with a hand-built event; (2) `mouseDoubleClick()` reads its own tracked last-mouse-position, not the double-click event's own position, so `QTest::mouseDClick()` needs a priming `QTest::mouseClick()` at the same point first; (3) `ConnectionManager::detach()`'s "delete old wire, immediately start a new in-progress one from the same source" behavior defeats a naive connection-count assertion — check the specific port/wire objects instead. No production bugs found; every gap was a real missing test. Directory total for this file: 113/113, 100%.
