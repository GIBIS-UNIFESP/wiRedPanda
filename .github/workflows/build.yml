# Copyright 2015 - 2026, GIBIS-Unifesp and the wiRedPanda contributors
# SPDX-License-Identifier: GPL-3.0-or-later

name: Build

on:
  push:
    branches-ignore:
      - master
      - wasm

  pull_request:
    branches:
      - master
    types: [opened, synchronize, reopened]

jobs:
  build:
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        qt: [5.15.2, 6.2.4, 6.8.3, 6.10.2]
        cmake_args: ['']
        include:
          - os: ubuntu-latest
            qt: '6.8.3'
            cmake_args: '-DENABLE_UNITY_BUILD=OFF -DENABLE_PCH=OFF'

# =================================

    runs-on: ${{ matrix.os }}
    name: ${{ matrix.os }} Qt${{ matrix.qt }}${{ matrix.cmake_args != '' && ' (no unity/PCH)' || '' }}
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name != github.event.pull_request.base.repo.full_name

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Install Qt
      uses: jurplel/install-qt-action@v4
      with:
        version: ${{ matrix.qt }}
        cache: true
        aqtversion: ==3.*
        modules: ${{ startsWith(matrix.qt, '6') && 'qtmultimedia qtimageformats' || '' }}

    - name: ccache
      uses: hendrikmuhs/ccache-action@v1
      with:
        key: ${{ matrix.os }}-${{ matrix.qt }}${{ matrix.cmake_args != '' && '-no-unity' || '' }}

    - name: Setup Windows MSVC Toolchain
      if: runner.os == 'Windows'
      uses: seanmiddleditch/gha-setup-vsdevenv@v5

# =================================

    - name: Build
      run: |
        cmake --preset release ${{ matrix.cmake_args }}
        cmake --build --preset release

    - name: Test
      timeout-minutes: 10
      env:
        QT_FORCE_STDERR_LOGGING: 1
      run: |
        ctest --preset release -V

    - name: Upload build artifacts on failure
      if: failure()
      uses: actions/upload-artifact@v6
      with:
        name: build-logs-${{ matrix.os }}-${{ matrix.qt }}${{ matrix.cmake_args != '' && '-no-unity' || '' }}
        path: |
          build/CMakeCache.txt
          build/CMakeFiles/CMakeOutput.log
          build/CMakeFiles/CMakeError.log
        retention-days: 7
